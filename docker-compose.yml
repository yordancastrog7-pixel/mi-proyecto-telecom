version: '3.8'

services:
  db:
    image: mysql:5.7
    container_name: telecom_db
    restart: always
    environment:
      MYSQL_DATABASE: proyecto_telecom
      MYSQL_ROOT_PASSWORD: rootpassword
    ports:
      - "3307:3306"
    volumes:
      - ./data:/var/lib/mysql